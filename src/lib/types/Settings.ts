export interface LocalSettings {
	isDarkmode: boolean;
}

export const DEFAULT_LOCALSETTINGS: LocalSettings = {
	isDarkmode: false
};

export interface GlobalSettings {}

export const DEFAULT_GLOBALSETTINGS: GlobalSettings = {};
